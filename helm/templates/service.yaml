apiVersion: v1
kind: Service
metadata:
  name: {{ include "gameplay.fullname" . }}
  labels:
    {{- include "gameplay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gameplay.selectorLabels" . | nindent 4 }}
