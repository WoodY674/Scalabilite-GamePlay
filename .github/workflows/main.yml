name: 💫 Vercel Production Deployment

on:
  pull_request:
    types:
      - closed

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  DOCKER_IMAGE_NAME: scalabilite-gameplay-front
  DOCKER_IMAGE_TAG: latest

jobs:
  Build-Test-Image:
    if: github.event.pull_request.merged == true
    name: 🐳 Build Test Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Test Docker Image
        run: docker build -t ${{ env.DOCKER_IMAGE_NAME }}:$GITHUB_RUN_ID -f Dockerfile .

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Push Docker Image to Docker Hub
        run: docker push anthlr/${{ env.DOCKER_IMAGE_NAME }}:$GITHUB_RUN_ID

  Tests:
    name: 🧩 Run Tests
    runs-on: ubuntu-latest
    needs: Build-Test-Image
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Pull Test Docker Image
        run: docker pull anthlr/${{ env.DOCKER_IMAGE_NAME }}:$GITHUB_RUN_ID

      - name: Run Tests in Docker Container
        run: docker run --rm anthlr/${{ env.DOCKER_IMAGE_NAME }}:$GITHUB_RUN_ID npm run test

  Push-Docker-Image:
    name: 🐳 Push Docker Image to Docker Hub
    needs: [Tests]
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Push Docker Image to Docker Hub
        run: docker push anthlr/${{ env.DOCKER_IMAGE_NAME }}:${{ env.DOCKER_IMAGE_TAG }}
